<Window x:Class="SDSoftware.RevitTest.Features.Diagnostics.Views.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Model Probe"
        Height="760"
        Width="1080"
        FontFamily="Segoe UI"
        FontSize="12"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False">
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0"
		           Margin="0,0,0,8"
		           TextWrapping="Wrap"
		           Text="Select all (Ctrl+A) and copy, or use the buttons below. Send this text back so the tool can be built against the real model." />

		<TextBox x:Name="LogBox"
		         Grid.Row="1"
		         IsReadOnly="True"
		         FontFamily="Consolas"
		         FontSize="12"
		         TextWrapping="NoWrap"
		         AcceptsReturn="True"
		         VerticalScrollBarVisibility="Auto"
		         HorizontalScrollBarVisibility="Auto" />

		<StackPanel Grid.Row="2"
		            Orientation="Horizontal"
		            HorizontalAlignment="Right"
		            Margin="0,10,0,0">
			<TextBlock x:Name="StatusText"
			           VerticalAlignment="Center"
			           Margin="0,0,12,0"
			           Foreground="#FF6B6B6B" />
			<Button x:Name="CopyButton"
			        Content="Copy to clipboard"
			        MinWidth="130"
			        Height="26"
			        Margin="0,0,8,0"
			        Click="OnCopyClick" />
			<Button x:Name="SaveButton"
			        Content="Save as file..."
			        MinWidth="110"
			        Height="26"
			        Margin="0,0,8,0"
			        Click="OnSaveClick" />
			<Button Content="Close"
			        MinWidth="84"
			        Height="26"
			        IsCancel="True" />
		</StackPanel>
	</Grid>
</Window>
